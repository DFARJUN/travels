# הוראות עריכה — אתרי טיול אינטראקטיביים

> **חוקים תקפים לכל פעם שדור מבקש אתר אינטראקטיבי עם תוכנית של טיול.** לקרוא ולפעול לפיהם לפני בניית/עריכת כל אתר טיול.

## ⭐ הכלל הראשון: תמיד לבנות מהטמפלייט, לא מאפס
כל אתר טיול נבנה **על בסיס הטמפלייט הקבוע** — לא כותבים HTML/CSS חדש מאפס.

- **הטמפלייט:** תיקיית `personal/trips/_trip-site-template/` — הקובץ לעריכה הוא `trip-site-template.dc.html`, לצד `image-slot.js` / `route-map.js` / `support.js` (מנוע הרכיב, משבצות תמונה, מפת המסלול). יש שם גם `README.md` עם הסבר מלא.
- לטיול חדש: מעתיקים את **כל התיקייה** לתיקיית הטיול, ומחליפים ב־`.dc.html` **רק את מערכי הנתונים** (`DAYS`, `BASES`, `BOOKINGS`, `HIGHLIGHTS`, `NOTES`, צ'קליסט) + התמונות (ויקימדיה) + טקסטים סטטיים בתבנית. לא נוגעים במנוע/עיצוב.
- הטמפלייט כולל: טיימליין "איפה ישנים" (גודל לפי לילות), מפה שמבדילה לינה מביקור, והזמנות עם פופאפ קוד אישור. פרסום standalone רק כשמבקשים (ראו `SOURCE-TO-STANDALONE.md`).
- כלל התמונות, התאריכים, וכל שאר החוקים למטה חלים תמיד.

## ⭐ הכלל השני: אתר פעיל = תמיד לסנכרן
אם לטיול יש **אתר פעיל/חי** (למשל מפורסם ב-Netlify או בכל אירוח אחר) — **כל מידע חדש שמתעדכן במסלול חייב להתעדכן גם באתר החי**, לא רק בקבצים המקומיים.

- כל שינוי בתוכן (יום, בסיס, הזמנה, צ'קליסט, תמונה) → לעדכן את הקובץ **וגם** לפרסם מחדש את האתר החי.
- לשמור על **אותה כתובת** של האתר החי (deploy לאותו site), לא ליצור לינק חדש בכל פעם.
- לוודא שהשינוי באמת עלה (לבדוק את הכתובת החיה אחרי הפרסום).
- מיקום האתר החי של כל טיול נשמר בקובץ המסלול של אותו טיול (`itinerary.md`).
- **ריפו הטראוול — תמיד לעדכן בסוף.** כל שינוי מקומי בקבצי הטיולים חייב להתעדכן גם בריפו `github.com/DFARJUN/travels` (push לענף `main`), בסוף כל עבודה בהקשר של טיולים. זה שלב חובה, לא אופציונלי.

## ⭐ הכלל השלישי: כל אתר טיול חדש נוסף לדף הראשי
יש דף אינדקס ראשי בשורש התיקייה: `personal/trips/index.html` - כרטיסייה לכל טיול, בסגנון אתרי הטיול (אותו גופן, פלטה, RTL).

- **בכל פעם שנבנה אתר טיול חדש (`.dc.html`) - מוסיפים לו כרטיס ב-`index.html`.** לא מסיימים בלי זה.
- הכרטיס מקשר לקובץ ה-`.dc.html` (קובץ המקור), **לא** לקובץ ה-standalone.
- מיקום ההוספה: בתוך `<div class="grid" id="trips">`, אחרי הערת ה-`TRIP CARDS`. מעתיקים כרטיס קיים ומעדכנים: `href` (עם רווחים מקודדים כ-`%20`), תמונת רקע (ויקימדיה, לוודא שנטענת), תאריך, כותרת, ותיאור קצר.
- אם עדכנו כותרת/תאריכים של טיול קיים - לעדכן גם את הכרטיס בדף הראשי.

---

מדריך לעריכת קובץ המסלול `Sicilia 2026.dc.html`. הקובץ בנוי כ־Design Component: יש בו **תבנית** (ה־HTML) ו**מחלקת לוגיקה** (ה־JavaScript, `class Component`), ורוב התוכן יושב במערכי נתונים בתחתית הקובץ — שם עורכים.

---

## כלל זהב: תמונות תמיד מהאינטרנט
אני (קלוד) לא מייצר תמונות. כל תמונה בקובץ היא **קישור לתמונה אמיתית מהאינטרנט** (ויקימדיה קומונס), או תמונה שאתם גוררים בעצמכם למשבצת.

- כל משבצת תמונה היא רכיב `<image-slot>` עם תכונת `src` שמצביעה על כתובת אינטרנט.
- **אפשר תמיד להחליף** תמונה ידנית: לגרור קובץ תמונה מהמחשב ישר על המשבצת בתצוגה, או ללחוץ עליה ולבחור קובץ. גרירה גוברת על ה־`src` המקורי.
- כשמוסיפים תמונה חדשה דרך קוד — לוקחים כתובת מוויקימדיה קומונס בפורמט:
  `https://commons.wikimedia.org/wiki/Special:FilePath/<שם הקובץ>?width=900`
  (רווחים מקודדים כ־`%20`). תמיד להוסיף `credit` ו־`credit-href` שמפנים לעמוד הקובץ.
- **חובה לוודא שהתמונה באמת נטענת** — לא כל שם קובץ קיים. אחרי הוספה, בודקים שהתמונה מופיעה (ולא ריבוע ריק).

---

## כלל התאריכים
תמיד לכתוב תאריך **עם החודש**, בשני צידי הטווח. לא `18–20.9` אלא `18.9–20.9`.
תאריך בודד תמיד כולל חודש: `17.9`. שנה מלאה רק בהזמנות: `17.9.2026`.

---

## איפה עורכים מה — מערכי הנתונים (בתחתית הקובץ, בתוך ה־`<script>`)

### `DAYS` — ימי המסלול
כל יום הוא אובייקט. שדות עיקריים:
- `dayLabel` / `dateLabel` — כותרת היום והתאריך (למשל `'שישי 18.9'`).
- `title` / `meta` / `tags` — כותרת, שורת מידע קצרה, ותגיות.
- `label` — המספר שמופיע על הסיכה במפה.
- `kind` — סוג הנקודה: `'embark'` (התחלה), `'port'` (עצירה), `'disembark'` (סיום).
- `place`, `lat`, `lng` — שם המקום והקואורדינטות **למפה** (חובה מדויקות אחרת הסיכה תזוז).
- `blocks` — תוכנית היום, מערך של `{when, text}` (בוקר / צהריים / ערב וכו').
- `note` — הערה אופציונלית בתחתית היום.

לחיצה על סיכה במפה קופצת ליום המתאים — לכן ה־`lat/lng` וסדר הימים חייבים להישאר תואמים.

### `BASES` — בסיסי הלינה (הכרטיסים עם התמונות)
כל בסיס: `name`, `nights` (מספר לילות), `dates` (טווח עם חודש), `covers` (מה עושים ממנו), ותמונה: `img`, `credit`, `creditHref`, `slotId` (מזהה ייחודי — לא לשנות סתם).

### `BOOKINGS` — טיסות, רכב, לינה, כרטיסים
מקובץ לפי קטגוריות. כל שורה: `title`, `detail`, `when` (תאריך). כאן כותבים שנה מלאה בטיסות/רכב.

### צ'קליסט
רשימת הכנה לפני הנסיעה, מקובצת לפי נושא. מצב הסימון נשמר אוטומטית בדפדפן (localStorage).

---

## כללי עריכה כלליים שאני עובד לפיהם
- **שינוי קטן = שינוי קטן בלבד.** אם מבקשים לשנות טקסט/צבע/פריט אחד — אני נוגע רק בו ולא "משפר" דברים שלא ביקשתם.
- **טקסט שאתם נותנים — נשאר מילה במילה.** אני לא משכתב תוכן שלכם אלא אם תבקשו.
- **עברית ו־RTL:** כל העימוד מיושר לימין (`dir="rtl"`). טקסט באנגלית (שמות מקומות, מסעדות) מותר לשלב.
- **צבעים וגופן:** הגופן הוא Assistant; פלטת הצבעים חמה (רקע לבן, כתום־חלוד `#b8452b` להדגשות, כחול־פחם כהה). לא מוסיפים צבעים חדשים אקראית.
- **מינימליזם:** לא ממלאים את הדף בקישוטים, אייקונים או מספרים מיותרים. כל אלמנט צריך להצדיק את מקומו.
- **אם אני חושב שכדאי להוסיף משהו** (עוד יום, עוד סעיף) — אני שואל אתכם קודם, לא מוסיף מעצמי.

---

## ייצוא לקובץ עצמאי (standalone)
הקובץ `Sicilia 2026.html` הוא גרסה עצמאית: הגופנים, הסקריפטים וה־CSS מוטמעים בפנים ועובדים אופליין.
**שימו לב:** 6 תמונות הנוף נשארות קישור חי לוויקימדיה — הן נטענות רק כשיש אינטרנט. בלי רשת הן יופיעו ריקות. אחרי כל עריכה של הקובץ המקורי צריך לייצא מחדש כדי לעדכן את הגרסה העצמאית.

---

## קבצים בפרויקט
- `Sicilia 2026.dc.html` — **הקובץ הראשי לעריכה.**
- `Sicilia 2026.html` — הגרסה העצמאית להורדה/שיתוף (מיוצרת מחדש מהראשי).
- `image-slot.js`, `route-map.js`, `support.js` — רכיבי עזר (משבצות תמונה, מפת המסלול, מנוע הרכיב). לא לגעת אלא אם צריך.

---

## טאבים ותכונות סטנדרטיות (בכל אתר טיול)
כל אתר טיול כולל חמישה טאבים: **סקירה · המסלול · צ'קליסטים · הזמנות · הוצאות**. הנתונים במערכים בתחתית הקובץ.

- **שדה לינה קבוע (`meta` בכל יום):** בטאב המסלול, כל יום מציג רק את הלינה של אותו לילה: `לינה ב{עיר} (לילה/סה"כ)`, עם שם המלון כשידוע (`לינה במלון X - עיר (1/4)`), ו־`טיסה הביתה` ביום החזרה. בלי טקסט נסיעה/סיור בשדה הזה.
- **מפה — כפתור "לינה בלבד":** מעל המפה יש מתג `כל היעדים / לינה בלבד`. במצב "לינה בלבד" המפה מציגה רק את נקודות הלינה, כל נקודה עם מספר היעד ובדג' של מספר הלילות. הנתונים במערך `STAYS` (`{ id, num, name, nights, lat, lng }`) — נקודה אחת לכל בסיס, לפי הסדר.
- **הזמנות — קישור לבוקינג:** כל שורת לינה עם `href` מציגה כפתור "בוקינג ↗" (חיפוש בוקינג לפי שם המלון+עיר). לינה ללא מלון אמיתי (בית של חבר / טרם הוזמן) — בלי `href`.
- **הוצאות — שלושה מצבים לכל שורה (`EXPENSES.categories[].rows`):**
  - סכום ידוע → מספר ב־`amount` (מוצג `₪…`).
  - **חסר** (אדום) → `amount:null` על פריט **שכבר הוזמן** אבל אין עלות.
  - **לא הוזמן** (צהוב) → `amount:null, booked:false` על פריט שעדיין לא הוזמן / אין מידע.
  - בטיול שכבר קרה, הכול נחשב "הוזמן" → אין צהוב. רק בטיול עתידי מסמנים `booked:false`.
- **טאב ב־URL:** הטאב הפעיל נשמר כ־`?tab=` ומשוחזר ברענון.

## המרות מטבע (חובה)
כשממירים מטבע זר לשקל — **תמיד לבדוק את שער החליפין בפועל ביום ההזמנה (יום ההזמנה המשוער), לא שער מנוחש.** לשמור את הסכום המקורי בתווית (למשל `($312.17)` / `(€806.96)`) ואת הסכום המומר ב־`amount`.
